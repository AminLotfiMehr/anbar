# دستورات ریستارت سرویس و سرور

## 📋 دستورات سریع

### 1️⃣ ریستارت سرویس PM2 (اپلیکیشن)

```bash
# ریستارت همه سرویس‌های PM2
pm2 restart all

# ریستارت سرویس خاص (inventory-app)
pm2 restart inventory-app

# ریستارت با استفاده از ecosystem.config.js
pm2 restart ecosystem.config.js
```

**دستورات مرتبط:**
```bash
# مشاهده وضعیت
pm2 status

# مشاهده لاگ‌ها
pm2 logs inventory-app

# توقف سرویس
pm2 stop all

# شروع سرویس
pm2 start all
```

---

### 2️⃣ ریستارت سرویس‌های Docker (دیتابیس و Adminer)

```bash
# ریستارت همه سرویس‌های Docker Compose
docker compose restart

# یا با docker-compose (نسخه قدیمی)
docker-compose restart

# ریستارت سرویس خاص (دیتابیس)
docker compose restart db

# ریستارت سرویس خاص (Adminer)
docker compose restart adminer
```

**دستورات مرتبط:**
```bash
# مشاهده وضعیت
docker compose ps

# مشاهده لاگ‌ها
docker compose logs -f

# توقف سرویس‌ها
docker compose stop

# شروع سرویس‌ها
docker compose start
```

---

### 3️⃣ ریستارت کامل (همه سرویس‌ها)

```bash
# ریستارت Docker Compose و PM2 به صورت یکجا
docker compose restart && pm2 restart all
```

**یا استفاده از اسکریپت:**
```bash
./restart-all.sh
```

---

### 4️⃣ ریستارت سرور (سیستم عامل)

```bash
# ریستارت سرور
sudo reboot

# یا با shutdown
sudo shutdown -r now

# ریستارت با تاخیر (مثلاً 5 دقیقه)
sudo shutdown -r +5

# لغو ریستارت برنامه‌ریزی شده
sudo shutdown -c
```

---

## 🚀 اسکریپت‌های آماده

### نمایش همه دستورات
```bash
./restart-commands.sh
```

### ریستارت خودکار همه سرویس‌ها
```bash
./restart-all.sh
```

---

## 📊 دستورات مانیتورینگ

```bash
# مشاهده استفاده از منابع PM2
pm2 monit

# مشاهده لاگ‌های زنده
pm2 logs --lines 100

# مشاهده وضعیت Docker
docker compose ps
docker stats
```

---

## 🔧 دستورات مفید دیگر

```bash
# ذخیره تنظیمات PM2
pm2 save

# تنظیم startup خودکار PM2
pm2 startup

# مشاهده اطلاعات سرویس PM2
pm2 info inventory-app

# مشاهده لاگ‌های Docker
docker compose logs db
docker compose logs adminer
```

---

## ⚠️ نکات مهم

1. **قبل از ریستارت سرور**: مطمئن شوید که PM2 startup تنظیم شده است
   ```bash
   pm2 startup
   pm2 save
   ```

2. **بعد از ریستارت**: بررسی کنید که همه سرویس‌ها در حال اجرا هستند
   ```bash
   pm2 status
   docker compose ps
   ```

3. **در صورت مشکل**: لاگ‌ها را بررسی کنید
   ```bash
   pm2 logs
   docker compose logs
   ```

---

## 📍 مسیر پروژه

```
/home/isaco/inventory-app
```

---

## 🔗 آدرس‌های دسترسی

- **Backend API (داخلی)**: http://192.168.1.151:3000
- **Backend API (خارجی)**: http://185.120.251.246:3000
- **Adminer (داخلی)**: http://192.168.1.151:8080
- **Adminer (خارجی)**: http://185.120.251.246:8080

