# 🚀 راهنمای سریع استقرار پروژه

## ✅ مراحل انتقال فایل‌ها به سرور

### 1️⃣ فایل‌هایی که باید به سرور منتقل شوند:

```
همه فایل‌ها و پوشه‌های زیر را به سرور منتقل دهید:

✅ پوشه backend/ (کل پوشه)
✅ package.json
✅ tsconfig.json
✅ server.ts
✅ bun.lock
```

### 2️⃣ مسیر نصب در سرور:

```bash
# در سرور Ubuntu خود:
mkdir -p ~/inventory-app
cd ~/inventory-app
```

### 3️⃣ انتقال فایل‌ها با SCP:

```bash
# در کامپیوتر خود (Windows PowerShell یا Git Bash):
cd مسیر/پروژه/شما

# انتقال فایل‌ها:
scp -P 2223 -r backend package.json tsconfig.json server.ts bun.lock root@185.120.251.246:~/inventory-app/
```

### 4️⃣ نصب و اجرا در سرور:

```bash
# در سرور:
cd ~/inventory-app

# نصب Bun (اگر نصب نشده):
curl -fsSL https://bun.sh/install | bash
source ~/.bashrc

# نصب dependencies:
bun install

# تست اجرا:
bun run server.ts
```

باید پیام زیر را ببینید:
```
[Server] Starting on port 3000...
[Server] Server is running on http://localhost:3000
```

### 5️⃣ تنظیم اجرای دائمی با PM2:

```bash
# نصب PM2:
npm install -g pm2

# اجرای سرور:
pm2 start server.ts --interpreter bun --name inventory-backend

# ذخیره و راه‌اندازی خودکار:
pm2 save
pm2 startup
```

### 6️⃣ تنظیم Firewall:

```bash
sudo ufw allow 3000/tcp
sudo ufw enable
```

### 7️⃣ تنظیم Port Forwarding در مودم:

در تنظیمات مودم خود یک قانون Port Forwarding جدید اضافه کنید:

```
نام: Inventory API
پورت خارجی: 3000
پورت داخلی: 3000
IP داخلی: 192.168.1.151
پروتکل: TCP
```

---

## 🧪 تست اتصال

### از داخل شبکه:
```bash
curl http://192.168.1.151:3000/
curl http://192.168.1.151:3000/health
```

### از خارج شبکه (موبایل با 4G):
```bash
curl http://185.120.251.246:3000/
curl http://185.120.251.246:3000/health
```

اگر موفق بود، باید این پیام را ببینید:
```
سرور انبارداری در حال اجرا است
برای استفاده از API از اپلیکیشن موبایل استفاده کنید.
```

---

## 📱 تنظیم اپلیکیشن

وقتی سرور راه‌اندازی شد، در اپلیکیشن موبایل:

1. صفحه تنظیمات شبکه را باز کنید (چرخ دنده)
2. آدرس سرور: `http://185.120.251.246:3000`
3. دکمه "تست اتصال" را بزنید
4. اگر موفق بود، "ذخیره" را بزنید

---

## 🔧 دستورات مفید PM2

```bash
# مشاهده وضعیت:
pm2 status

# مشاهده لاگ‌ها:
pm2 logs inventory-backend

# راه‌اندازی مجدد:
pm2 restart inventory-backend

# توقف:
pm2 stop inventory-backend
```

---

## 📱 درباره حالت نصبی (APK)

برای ساخت فایل APK، باید از EAS Build استفاده کنید:

```bash
# در کامپیوتر خود:
npm install -g eas-cli
eas login
eas build --platform android
```

⚠️ **توجه**: این نیاز به حساب Expo دارد و ممکن است هزینه داشته باشد.

---

## ✅ چک‌لیست نهایی

- [ ] فایل‌ها به سرور منتقل شدند
- [ ] Bun نصب شد
- [ ] `bun install` اجرا شد
- [ ] سرور با PM2 اجرا شد
- [ ] Firewall تنظیم شد (پورت 3000)
- [ ] Port Forwarding در مودم تنظیم شد
- [ ] تست از داخل شبکه موفق بود
- [ ] تست از خارج شبکه موفق بود
- [ ] اپلیکیشن موبایل به سرور متصل شد

---

## 🆘 رفع مشکلات رایج

### سرور اجرا می‌شود اما از خارج در دسترس نیست:
1. بررسی Port Forwarding مودم
2. بررسی Firewall: `sudo ufw status`
3. بررسی لاگ: `pm2 logs inventory-backend`

### خطای "EADDRINUSE":
```bash
sudo fuser -k 3000/tcp
pm2 restart inventory-backend
```

### سرور بعد از restart اجرا نمی‌شود:
```bash
pm2 startup
pm2 save
```
