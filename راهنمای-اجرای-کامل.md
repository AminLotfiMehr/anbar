# راهنمای اجرای کامل سیستم انبارداری

## معماری سیستم

این پروژه از **دو سرور مجزا** تشکیل شده است:

### 1️⃣ سرور Backend (API Server)
- **مسیر**: `backend/hono.ts` + `server.ts`
- **پورت پیش‌فرض**: 3000
- **وظیفه**: مدیریت API، دیتابیس، احراز هویت

### 2️⃣ سرور Frontend (Expo/React Native Web)
- **مسیر**: `app/` folder
- **پورت پیش‌فرض**: 8081 یا 8082
- **وظیفه**: نمایش رابط کاربری موبایل/وب

---

## 🚀 راه‌اندازی در سرور لینوکس

### مرحله 1: آماده‌سازی سرور

```bash
# نصب Node.js و Bun
curl -fsSL https://bun.sh/install | bash

# کپی فایل‌های پروژه به سرور
# از VSCode با SFTP یا FileZilla استفاده کنید
# مسیر توصیه شده: /home/your-user/warehouse-app
```

### مرحله 2: نصب Dependencies

```bash
cd /home/your-user/warehouse-app
bun install
```

### مرحله 3: اجرای Backend Server

```bash
# روش 1: اجرای مستقیم
bun run server.ts

# روش 2: اجرای با PM2 (توصیه می‌شود)
npm install -g pm2
pm2 start server.ts --interpreter bun --name warehouse-backend
pm2 save
pm2 startup
```

**Backend شما حالا روی `http://192.168.1.151:3000` در حال اجراست ✅**

### مرحله 4: اجرای Frontend Server (Expo)

در **ترمینال دوم**:

```bash
cd /home/your-user/warehouse-app

# ایجاد فایل .env برای تنظیمات
echo "EXPO_PUBLIC_API_URL=http://185.120.251.246:3000" > .env

# اجرای Expo server
bunx expo start --web --port 8082 --host 0.0.0.0
```

**Frontend شما حالا روی `http://192.168.1.151:8082` در حال اجراست ✅**

---

## 🌐 دسترسی از خارج شبکه

### تنظیمات Port Forwarding روی مودم:

| سرویس | پورت داخلی | پورت خارجی | IP داخلی |
|-------|------------|------------|-----------|
| Backend API | 3000 | 3000 | 192.168.1.151 |
| Frontend Web | 8082 | 8082 | 192.168.1.151 |

### لینک‌های دسترسی:

#### از داخل شبکه محلی:
- **Frontend**: http://192.168.1.151:8082
- **Backend**: http://192.168.1.151:3000

#### از خارج (اینترنت):
- **Frontend**: http://185.120.251.246:8082
- **Backend**: http://185.120.251.246:3000

---

## 📱 استفاده از اپلیکیشن

### روش 1: دسترسی از مرورگر موبایل
```
http://185.120.251.246:8082
```

### روش 2: اسکن QR Code
وقتی `bunx expo start` را اجرا می‌کنید، یک QR Code نمایش داده می‌شود.
با اپلیکیشن **Expo Go** این کد را اسکن کنید.

### روش 3: ساخت APK نصبی

برای ساخت فایل APK:

```bash
# نصب EAS CLI
npm install -g eas-cli

# ورود به حساب Expo
eas login

# ساخت APK
eas build --platform android --profile preview
```

**نکته**: ساخت APK نیاز به حساب Expo دارد و ممکن است 15-20 دقیقه طول بکشد.

---

## ⚙️ تنظیمات Network در اپ

کاربران می‌توانند از داخل اپ، در **صفحه تنظیمات شبکه**، آدرس سرور را تغییر دهند:

1. باز کردن اپ
2. کلیک روی آیکون ⚙️ در گوشه صفحه
3. وارد کردن:
   - **Server IP**: 185.120.251.246
   - **Port**: 3000
4. تست اتصال
5. ذخیره

---

## 🔧 عیب‌یابی مشکلات رایج

### مشکل 1: صفحه "سرور انبارداری در حال اجرا است"
**علت**: شما به Backend Server دسترسی دارید اما Frontend Server اجرا نشده.

**راه‌حل**: 
```bash
bunx expo start --web --port 8082 --host 0.0.0.0
```

سپس به `http://192.168.1.151:8082` بروید.

---

### مشکل 2: CORS Error
**علت**: Frontend و Backend روی دامین‌های متفاوت هستند.

**راه‌حل**: Backend شما قبلاً CORS را فعال کرده است ✅

---

### مشکل 3: نام کاربری ذخیره نمی‌شود
**علت**: مشکل در ذخیره‌سازی دیتابیس یا مسیر فایل

**راه‌حل**: اطمینان حاصل کنید:
```bash
# دسترسی نوشتن به پوشه پروژه
chmod -R 755 /home/your-user/warehouse-app
```

---

### مشکل 4: فایل اکسل آپلود نمی‌شود
**علت**: 
- کدهای با صفر شروع می‌شوند (Excel آنها را عدد می‌داند)
- مشکل در encoding فارسی

**راه‌حل**: این مشکل در کد برطرف خواهد شد.

---

## 📋 چک‌لیست نهایی

- [ ] Backend Server اجرا شده (پورت 3000)
- [ ] Frontend Server اجرا شده (پورت 8082)
- [ ] Port Forwarding تنظیم شده
- [ ] تست اتصال از داخل شبکه موفق
- [ ] تست اتصال از خارج شبکه موفق
- [ ] ثبت‌نام و ورود کاربر کار می‌کند
- [ ] آپلود فایل اکسل کار می‌کند

---

## 📞 دستورات مفید

```bash
# مشاهده لاگ Backend
pm2 logs warehouse-backend

# ری‌استارت Backend
pm2 restart warehouse-backend

# متوقف کردن همه سرویس‌ها
pm2 stop all

# لیست سرویس‌های در حال اجرا
pm2 list

# مشاهده استفاده از منابع
pm2 monit
```

---

## ⚠️ نکات امنیتی

1. **Firewall**: فقط پورت‌های 3000 و 8082 را باز کنید
2. **HTTPS**: برای production از SSL استفاده کنید
3. **Authentication**: Token‌ها را امن نگه دارید
4. **Database Backup**: به طور منظم از دیتابیس backup بگیرید

---

## 📱 پنل مدیریتی (مرحله بعد)

برای ساخت پنل مدیریت وب، می‌توان:

1. یک روت `/admin` در Expo ایجاد کرد
2. یا یک پروژه Next.js جداگانه برای ادمین ساخت
3. از همان Backend API استفاده کند

آیا می‌خواهید پنل ادمین را همین الان بسازیم؟
